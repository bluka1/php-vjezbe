<?php
    $a = 10;
    $b = '10';
    
    // Varijabla a je jednaka varijabli b (true)
    var_dump ($a == $b);
    // Varijabla a nije identična varijabli b (false)
    var_dump ($a === $b);
    // Varijabla a nije identična varijabli b (false)
    var_dump ($a != $b);
    // Varijabla a nije različita od varijable b (false)
    var_dump ($a < $b);
    // Varijabla a nije identična varijabli b (false)
    var_dump ($a > $b);
    // Varijabla a je jednaka varijabli b (true)
    var_dump ($a <= $b);
    // Varijabla a je jednaka varijabli b (true)
    var_dump ($a >= $b);
?>